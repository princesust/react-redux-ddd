import React from 'react'

import ShelfContainer from 'books/interface/patterns/ShelfContainer'

export default { title: 'Book/Patterns/ShelfContainer' }

export const standard = (): React.ReactElement => <ShelfContainer>Something within the container</ShelfContainer>
