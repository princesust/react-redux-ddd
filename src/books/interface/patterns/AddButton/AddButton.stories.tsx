import React from 'react'

import AddButton from 'books/interface/patterns/AddButton'

export default { title: 'Book/Patterns/AddButton' }

export const standard = (): React.ReactElement => <AddButton>Add more books</AddButton>
