import styled from 'styled-components'

export const FilterSection = styled.div`
  display: flex;
  gap: 20px;

  .MuiFormControl-root {
    width: 120px;

    &:first-of-type {
      width: 200px;
    }
  }
`
